import { LoadingSpinner } from "@/components/ui/loading-spinner";

export default function CustomersLoading() {
  return <LoadingSpinner label="Loading customers…" size="lg" className="min-h-[320px]" />;
}
